export const PAIR_TTL_MS=600000,PAIR_STORAGE_KEY="astra-key-pair",PAIR_COOKIE_KEY="astra-key-pair";export type PairSession={firstCode:string;firstSign:string;startedAt:number;expiresAt:number};export const isPairSessionActive=(s:PairSession)=>s.expiresAt>Date.now();export const isPairSessionValid=isPairSessionActive;export const createPairSession=(firstCode:string,firstSign:string):PairSession=>({firstCode,firstSign,startedAt:Date.now(),expiresAt:Date.now()+PAIR_TTL_MS});/** Demo only: base64 JSON is not secure; use signed server tokens in production. */export const createPairToken=(p:PairSession)=>Buffer.from(JSON.stringify(p)).toString("base64url");