import {getAstronomyForDate} from "@/lib/astronomy/astronomy-service";import {createDailyReading} from "@/lib/astrology/daily-reading-engine";import {getSignBySlug} from "@/lib/zodiac/signs";export function getDailyForSign(slug:string,date:Date|string=new Date()){const sign=getSignBySlug(slug);if(!sign)return null;const astronomy=getAstronomyForDate(date);return{sign,astronomy,astro:{moonPhase:astronomy.moonPhaseName},reading:createDailyReading(sign,astronomy)}}